name: CI

on:
  push:
    branches: [main, develop]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [24]
    steps:
      - uses: actions/checkout@v5

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Unit & integration tests
        run: npm test

      - name: Build
        run: npm run build

  uat:
    runs-on: ubuntu-latest
    continue-on-error: true
    needs: ci
    steps:
      - uses: actions/checkout@v5

      - name: Use Node.js 24
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      # Matches test:uat:ci (chromium-desktop only). Full matrix: npx playwright install --with-deps && npm run test:uat
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run UAT tests
        run: npm run test:uat:ci
        env:
          CI: true
          NODE_ENV: test
          # Same as tests/uat/utils/resolve-base-url.js + playwright webServer.url (webpack-dev-server)
          PLAYWRIGHT_TEST_BASE_URL: http://localhost:8080
          PLAYWRIGHT_CHROMIUM_FLAGS: --use-gl=swiftshader
